这些状态分为三大类：建立连接（三次握手）、数据传输、以及 断开连接（四次挥手）
# 建立连接阶段
* CLOSED: 初始状态，表示套接字未活动。
* LISTEN: 服务器调用 listen() 后进入的状态，表示“正在营业”，等待客户端连接。
* SYN_SENT: 客户端调用 connect() 发起连接后的状态。
* SYN_RCVD: 服务器收到客户端的 SYN 包并回送 SYN+ACK 后的状态，表示连接正在半开发状态。
* ESTABLISHED: **数据传输状态**g。三次握手完成，双方可以开始发数据了。